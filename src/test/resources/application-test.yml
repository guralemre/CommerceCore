spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///commercecore_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: test-secret-key-not-for-production-use-only-min-256-bits
  expiration-ms: 3600000
